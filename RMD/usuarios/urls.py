from django.urls import path
#from .views import user_profile, delete_submission

urlpatterns = [
    #path('<int:user_id>', user_profile, name='user_profile'),
    #path('delete/<int:lesson_file_id>', delete_submission, name='delete_submission')
]
